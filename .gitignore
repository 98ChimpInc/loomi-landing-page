# clasp credentials (contains script ID)
scripts/.clasp.json

# OS files
.DS_Store

# Node
node_modules/

# Firebase deploy cache. Written by `firebase deploy`, machine-local, and it
# grows with every deploy ... firebase.json and .firebaserc are the config and
# are tracked.
.firebase/
